---
slug: hello-world-cli
status: queued
created: 2026-06-10
---

# Hello World CLI

## 目标
做一个最简单的 Node.js CLI 工具,运行后打印 `hello world` 并退出。

## 验收标准
- 在终端执行后输出一行 `hello world`
- 立即退出(退出码 0)
- Node.js 实现

## 范围说明
用户明确表示没有其他细节要求 —— 不需要参数、不需要配置、不需要打包发布、不需要测试覆盖。最小可运行即可。
