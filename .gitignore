# Dependencies
node_modules/

# Environment / secrets
.env

# Runtime data
data/*.sqlite
data/*.sqlite-wal
data/*.sqlite-shm
data/*.sqlite.bak-*
data/snapshots/
data/runlog/
data/sandbox_proj_test/
data/test-sandbox/

# 本地启动脚本 (per-user)
start_*.ps1

# Probe artifacts (scripts themselves are committed, only logs ignored)
probe/log/
probe/runlog/

# Local Claude Code settings (per-user, not portable)
.claude/settings.local.json

# Logs
*.log
data/*.log

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# OS / build
*.tsbuildinfo
dist/
build/

# Agent work artifacts (R/H/B/C may create these inside the active project root
# when mate is running in dev mode with default = self project).
# Real sibling projects should not gitignore these — they own their workflow.
doc/
tools/
spike-*/

# release notes scratch files
.release-notes-*.md

.z常用命令.md
# E2E test artifacts
test-results/
playwright-report/
